run_id: "20260307-174316"
task_id: "ccodex-memory-os-06-retrieval"
mode: "deterministic_unit"
scenarios:
  - id: "EV-001"
    description: "Retrieval results remain shadow/advisory only while keeping deterministic ranking and explanation traces"
    setup: "Run the focused `memory_os_retrieve` test filter after adding an explicit advisory-only disposition to the result"
    expected_outcome: "Focused retrieval tests pass and every result exposes advisory-only disposition"
    grader_type: "deterministic"
    grader_ref: "cargo test -p codex-core memory_os_retrieve -- --nocapture"
    pass_threshold: "all filtered tests pass"
    status: "pass"
    evidence_refs:
      - "codex-rs/core/src/memory_os/tests.rs"
      - "runs/ccodex_loop/20260307-174316/memory_os_retrieve.log"
