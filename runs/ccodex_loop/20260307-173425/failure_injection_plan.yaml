run_id: "20260307-173425"
task_id: "ccodex-memory-os-06-retrieval"
injections:
  - id: "FI-001"
    class: "timeout"
    target: "not in scope for pure in-process retrieval scoring"
    expected_detection: "none required"
    expected_recovery: "n/a"
  - id: "FI-002"
    class: "ambiguous_requirement"
    target: "Task 6 boundary between advisory retrieval and authoritative continuity state"
    expected_detection: "spec review confirms retrieval must stay shadow/advisory"
    expected_recovery: "keep retrieval results in TRACE/1 records only and leave canonical authority untouched"
  - id: "FI-003"
    class: "stale_memory"
    target: "resume/reconstruction path with newer memory snapshot than transcript"
    expected_detection: "reconstruct_history suite keeps canonical memory-plane authority ahead of stale prose"
    expected_recovery: "fallback to deterministic planes and transcript only as evidence, not authority"
  - id: "FI-004"
    class: "guardrail_bypass_attempt"
    target: "optional semantic scorer passed while semantic scoring remains disabled"
    expected_detection: "memory_os_retrieve_keeps_semantic_branch_disabled_by_default stays green"
    expected_recovery: "leave semantic branch disabled until explicitly enabled in a later task"
  - id: "FI-005"
    class: "auth_failure"
    target: "not in scope; retrieval is local-only"
    expected_detection: "none required"
    expected_recovery: "n/a"
  - id: "FI-006"
    class: "quota_exhaustion"
    target: "not in scope; no remote retrieval calls are performed"
    expected_detection: "none required"
    expected_recovery: "n/a"
  - id: "FI-007"
    class: "dependency_outage"
    target: "not in scope; retrieval path depends only on local Rust code and serde types"
    expected_detection: "none required"
    expected_recovery: "n/a"
  - id: "FI-008"
    class: "partial_persistence"
    target: "stored retrieval explanation records inside MemoryOsSnapshot"
    expected_detection: "typed snapshot includes retrievals field and assembly renders TRACE/1 only when records exist"
    expected_recovery: "leave snapshot valid even when retrievals are empty"
  - id: "FI-009"
    class: "malformed_output"
    target: "NaN importance/confidence values in candidate scoring"
    expected_detection: "memory_os_retrieve_sanitizes_non_finite_scores_for_deterministic_ranking stays green"
    expected_recovery: "normalize to 0.0 before ranking and explanation formatting"
  - id: "FI-010"
    class: "schema_or_doc_drift"
    target: "Task 6 plan/spec versus current worktree implementation"
    expected_detection: "run report cross-checks code paths against docs/plans/2026-03-06-ccodex-memory-os.md and docs/ccodex-memory-os.md"
    expected_recovery: "record drift explicitly and stop rather than silently widening scope"
