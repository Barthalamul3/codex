run_id: "20260307-173425"
task_id: "ccodex-memory-os-06-retrieval"
assurance_mode: "standard"
coverage_rationale: "This run is Task 6 closeout only, so focused retrieval and reconstruction suites are the blocking evidence; workspace-wide coverage is deferred to the plan's later validation sweep."
blocking_checks:
  lint:
    command: "not_run_in_this_closeout; no Rust source edits were made in this run"
    mode: "strict"
  typecheck:
    command: "cargo test -p codex-core memory_os_retrieve -- --nocapture"
    mode: "strict"
  tests:
    command: "cargo test -p codex-core memory_os_retrieve -- --nocapture && cargo test -p codex-core reconstruct_history -- --nocapture"
    require_all_pass: true
  coverage:
    command: "focused deterministic coverage via memory_os_retrieve and reconstruct_history suites"
    minimum_percent: 85
anti_mock_policy:
  enforce_real_path_tests: true
  forbidden_patterns:
    - "test doubles for core business logic"
    - "mocking integration boundaries without approved exception"
  approved_exceptions:
    - "PanicSemanticScorer in memory_os/tests.rs is a guardrail probe, not a retrieval logic mock; it only asserts the disabled-by-default semantic branch is never called"
immediate_failure_policy:
  stop_on_first_blocking_failure: true
  rca_required_for_blocking_failure: true
  rerun_from_clean_state_required: true
