run_id: "20260307-173425"
task_id: "ccodex-memory-os-06-retrieval"
mode: "standard"
scenarios:
  - id: "EV-001"
    description: "Deterministic lexical-first retrieval returns the newer and more important episodic match ahead of older and pragmatic matches."
    setup: "Run cargo test -p codex-core memory_os_retrieve -- --nocapture"
    expected_outcome: "memory_os_retrieve_prefers_lexical_matches_then_recency_and_importance passes with the expected explanation order and scores"
    grader_type: "deterministic"
    grader_ref: "codex-rs/core/src/memory_os/tests.rs::memory_os_retrieve_prefers_lexical_matches_then_recency_and_importance"
    pass_threshold: "all focused retrieval tests pass"
    status: "passed"
    evidence_refs:
      - "cargo test -p codex-core memory_os_retrieve -- --nocapture"
  - id: "EV-002"
    description: "Shadow-mode guardrail prevents semantic scorer execution by default even when a scorer is supplied."
    setup: "Run cargo test -p codex-core memory_os_retrieve -- --nocapture"
    expected_outcome: "memory_os_retrieve_keeps_semantic_branch_disabled_by_default passes without PanicSemanticScorer panicking"
    grader_type: "deterministic"
    grader_ref: "codex-rs/core/src/memory_os/tests.rs::memory_os_retrieve_keeps_semantic_branch_disabled_by_default"
    pass_threshold: "test passes"
    status: "passed"
    evidence_refs:
      - "cargo test -p codex-core memory_os_retrieve -- --nocapture"
  - id: "EV-003"
    description: "Reconstruction still treats retrieval traces as advisory prompt context and not canonical authority."
    setup: "Run cargo test -p codex-core reconstruct_history -- --nocapture"
    expected_outcome: "reconstruction suite remains green with TRACE/1 rendering and memory-plane authority behavior intact"
    grader_type: "deterministic"
    grader_ref: "codex-rs/core/src/codex/rollout_reconstruction_tests.rs"
    pass_threshold: "all reconstruct_history tests pass"
    status: "passed"
    evidence_refs:
      - "cargo test -p codex-core reconstruct_history -- --nocapture"
