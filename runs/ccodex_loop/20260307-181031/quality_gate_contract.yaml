run_id: "20260307-181031"
task_id: "ccodex-memory-os-06-retrieval"
assurance_mode: "lite"
coverage_rationale: "This run is a narrow deterministic-behavior fix in existing retrieval code; local verification centers on exact-output tests in codex-core."
blocking_checks:
  lint:
    command: "cd codex-rs && just fmt"
    mode: "strict"
  typecheck:
    command: "cd codex-rs && cargo test -p codex-core memory_os_retrieve -- --nocapture"
    mode: "strict"
  tests:
    command: "cd codex-rs && cargo test -p codex-core memory_os_retrieve -- --nocapture && cargo test -p codex-core"
    require_all_pass: true
  coverage:
    command: "cargo test output reviewed for targeted retrieval coverage; no standalone coverage tool was run in this worktree"
    minimum_percent: 85
anti_mock_policy:
  enforce_real_path_tests: true
  forbidden_patterns:
    - "test doubles for core retrieval ranking logic"
    - "mocking deterministic source normalization behavior"
  approved_exceptions: []
immediate_failure_policy:
  stop_on_first_blocking_failure: true
  rca_required_for_blocking_failure: true
  rerun_from_clean_state_required: true
