run_id: "20260307-180009"
task_id: "ccodex-memory-os-06-retrieval"
version: "1"
assurance_mode: "standard"
change_type: "shadow_retrieval_verification"
runtime_impact: false
packaging_changed: false
in_scope:
  files:
    - "docs/plans/2026-03-06-ccodex-memory-os.md"
    - "docs/ccodex-memory-os.md"
    - "codex-rs/core/src/memory_os.rs"
    - "codex-rs/core/src/memory_os/retrieve.rs"
    - "codex-rs/core/src/memory_os/tests.rs"
  systems:
    - "ccodex shadow retrieval interface"
    - "deterministic retrieval scoring"
    - "retrieval explanation traces"
out_of_scope:
  files:
    - "codex-rs/core/src/codex.rs"
    - "codex-rs/core/src/state/session.rs"
    - "codex-rs/core/src/codex/rollout_reconstruction.rs"
  systems:
    - "authoritative retrieval rollout"
    - "stock codex behavior"
    - "future embedding or reranker integration"
forbidden_operations:
  - "making retrieval authoritative"
  - "changing stock codex behavior"
  - "destructive git operations"
escalation_conditions:
  - "retrieval test failure"
  - "semantic branch enabled by default"
  - "non-deterministic ranking evidence"
change_budget:
  max_files: 11
  max_behavioral_surface: "task-6 retrieval interface only"
success_criteria:
  functional:
    - "retrieve_shadow_memory returns advisory-only results"
    - "explanations include explicit score-component rationale"
    - "semantic scoring remains disabled by default"
  reliability:
    - "same inputs produce deterministic ranking and rounded scores"
    - "non-finite scores are sanitized"
  security:
    - "no external service dependency is required"
  quality:
    lint_mode: "strict"
    typecheck_mode: "strict"
    coverage_threshold_percent: 85
    require_no_failing_tests: true
    anti_mock_policy:
      enabled: true
      exceptions_require_approval: true
