run_id: "20260307-180009"
task_id: "ccodex-memory-os-06-retrieval"
mode: "shadow_only"
scenarios:
  - id: "EV-001"
    description: "deterministic lexical retrieval ranks higher lexical matches first, then recency and importance"
    setup: "run the focused retrieval unit tests against the current worktree"
    expected_outcome: "the newer higher-importance episodic record outranks the older one and both outrank the weaker pragmatic match"
    grader_type: "deterministic"
    grader_ref: "codex-rs/core/src/memory_os/tests.rs::memory_os_retrieve_prefers_lexical_matches_then_recency_and_importance"
    pass_threshold: "exact match"
    status: "pass"
    evidence_refs:
      - "cargo test -p codex-core memory_os_retrieve -- --nocapture"
  - id: "EV-002"
    description: "selected retrieval items carry explanation traces"
    setup: "run the focused retrieval unit tests against the current worktree"
    expected_outcome: "rationale string includes lexical, recency, importance, and semantic status"
    grader_type: "deterministic"
    grader_ref: "codex-rs/core/src/memory_os/tests.rs::memory_os_retrieve_records_why_an_item_was_selected"
    pass_threshold: "exact match"
    status: "pass"
    evidence_refs:
      - "cargo test -p codex-core memory_os_retrieve -- --nocapture"
  - id: "EV-003"
    description: "semantic branch remains disabled by default"
    setup: "run the focused retrieval unit tests against the current worktree with a panic-on-use scorer fixture"
    expected_outcome: "semantic_branch_used remains false and the panic scorer is not invoked"
    grader_type: "deterministic"
    grader_ref: "codex-rs/core/src/memory_os/tests.rs::memory_os_retrieve_keeps_semantic_branch_disabled_by_default"
    pass_threshold: "exact match"
    status: "pass"
    evidence_refs:
      - "cargo test -p codex-core memory_os_retrieve -- --nocapture"
