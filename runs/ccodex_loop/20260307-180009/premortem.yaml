run_id: "20260307-180009"
task_id: "ccodex-memory-os-06-retrieval"
owner: "codex"
failures:
  - id: "PM-001"
    severity: "high"
    scenario: "retrieval becomes authoritative and overrides canonical state"
    detection: "API surface or tests show non-advisory disposition or canonical participation in ranking"
    prevention: "keep retrieval disposition fixed to advisory-only and rank only episodic/pragmatic candidates"
    recovery: "disable retrieval path and fall back to deterministic memory planes only"
    owner: "implementation"
  - id: "PM-002"
    severity: "high"
    scenario: "semantic scorer activates by default and changes retrieval behavior"
    detection: "default-config retrieval invokes semantic scorer or flips semantic_branch_used to true"
    prevention: "default config leaves enable_semantic_scoring false and tests exercise that guardrail"
    recovery: "revert to lexical-recency-importance only and rerun focused retrieval tests"
    owner: "implementation"
  - id: "PM-003"
    severity: "medium"
    scenario: "non-finite scores or tie order produce unstable ranking"
    detection: "explanations emit NaN/inf or the same fixture orders candidates inconsistently"
    prevention: "normalize malformed scores, round component values, and use explicit tie-break ordering"
    recovery: "sanitize scores and re-run deterministic retrieval coverage"
    owner: "implementation"
