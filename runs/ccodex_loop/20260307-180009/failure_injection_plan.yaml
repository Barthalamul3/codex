run_id: "20260307-180009"
task_id: "ccodex-memory-os-06-retrieval"
injections:
  - id: "FI-001"
    class: "guardrail_bypass_attempt"
    target: "semantic scorer default path"
    expected_detection: "panic scorer would fire if semantic retrieval were enabled by default"
    expected_recovery: "leave semantic branch disabled and surface semantic_branch_used=false"
    status: "pass"
    evidence_refs:
      - "codex-rs/core/src/memory_os/tests.rs::memory_os_retrieve_keeps_semantic_branch_disabled_by_default"
      - "cargo test -p codex-core memory_os_retrieve -- --nocapture"
  - id: "FI-002"
    class: "malformed_output"
    target: "retrieval score inputs"
    expected_detection: "non-finite importance or confidence is normalized instead of leaking NaN/inf into ranking"
    expected_recovery: "sanitize to zero and keep deterministic explanations"
    status: "pass"
    evidence_refs:
      - "codex-rs/core/src/memory_os/tests.rs::memory_os_retrieve_sanitizes_non_finite_scores_for_deterministic_ranking"
      - "cargo test -p codex-core memory_os_retrieve -- --nocapture"
