run_id: "20260307-183600"
task_id: "ccodex-memory-os-08-observability"
version: "1"
assurance_mode: "standard"
change_type: "observability_guardrail_verification"
runtime_impact: true
packaging_changed: false
in_scope:
  files:
    - "docs/plans/2026-03-06-ccodex-memory-os.md"
    - "docs/ccodex-memory-os.md"
    - "codex-rs/core/src/codex.rs"
    - "codex-rs/core/src/memory_os/assemble.rs"
    - "codex-rs/core/src/codex_tests.rs"
  systems:
    - "ccodex prompt injection observability"
    - "memory snapshot traceability"
    - "retrieval failure fallback safety"
out_of_scope:
  files:
    - "stock codex runtime paths"
    - "non-observability memory plane work"
    - "workspace-wide validation sweep"
  systems:
    - "semantic retrieval authority"
    - "ZeroClaw stable release promotion"
forbidden_operations:
  - "destructive_git_without_explicit_approval"
  - "changing stock codex behavior"
  - "claiming project-wide completion from task-local evidence"
escalation_conditions:
  - "observability traces mutate canonical state"
  - "focused observability tests fail"
  - "artifact validator fails after real evidence is added"
change_budget:
  max_files: 14
  max_behavioral_surface: "ccodex-only injection observability"
success_criteria:
  functional:
    - "INJECT/1 captures structured keep/drop decisions for prompt injection"
    - "invalid retrieval explanations are dropped without corrupting canonical context"
    - "saved session traces remain inspectable"
  reliability:
    - "ccodex-only counters exist for keep/drop totals and retrieval failures"
    - "failure-safe fallback preserves deterministic prompt assembly"
  security:
    - "no new external dependency is introduced"
  quality:
    lint_mode: "strict"
    typecheck_mode: "strict"
    coverage_threshold_percent: 85
    require_no_failing_tests: true
    anti_mock_policy:
      enabled: true
      exceptions_require_approval: true
