run_id: "20260307-183600"
task_id: "ccodex-memory-os-08-observability"
assurance_mode: "standard"
coverage_rationale: "Task 8 is a prompt-injection observability contract, so the relevant gate is focused execution of the ccodex observability tests plus artifact validation."
blocking_checks:
  lint:
    command: "cargo fmt --manifest-path codex-rs/Cargo.toml --all --check"
    mode: "strict"
  typecheck:
    command: "cd codex-rs && cargo test -p codex-core ccodex_memory_observability --no-run"
    mode: "strict"
  tests:
    command: "cd codex-rs && ./target/debug/deps/codex_core-8191e0311c205786 --nocapture ccodex_memory_observability"
    require_all_pass: true
  coverage:
    command: "cd codex-rs && ./target/debug/deps/codex_core-8191e0311c205786 --nocapture ccodex_memory_observability"
    minimum_percent: 85
anti_mock_policy:
  enforce_real_path_tests: true
  forbidden_patterns:
    - "mocking injection traces instead of asserting rendered prompt output"
    - "mocking canonical recovery instead of asserting preserved prompt context"
  approved_exceptions: []
immediate_failure_policy:
  stop_on_first_blocking_failure: true
  rca_required_for_blocking_failure: true
  rerun_from_clean_state_required: true
