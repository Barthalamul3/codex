run_id: "20260307-183600"
task_id: "ccodex-memory-os-08-observability"
owner: "codex"
failures:
  - id: "PM-OBS-001"
    severity: "high"
    scenario: "prompt injection drops or keeps records without durable rationale"
    detection: "focused observability test lacks structured INJECT/1 keep/drop output"
    prevention: "derive and persist structured InjectionTraceRecord entries before prompt assembly"
    recovery: "revert to pre-instrumented prompt path and re-run focused observability verification"
    owner: "implementation"
  - id: "PM-OBS-002"
    severity: "high"
    scenario: "non-finite retrieval explanations leak into prompt context or alter canonical state"
    detection: "focused observability test shows retrieval trace text in prompt or missing canonical context"
    prevention: "drop non-finite retrieval scores and record explicit structured DROP traces"
    recovery: "disable retrieval trace injection and fall back to deterministic planes only"
    owner: "implementation"
  - id: "PM-OBS-003"
    severity: "medium"
    scenario: "observability hooks affect stock codex behavior"
    detection: "code path is not gated to ccodex or tests show stock codex prompt drift"
    prevention: "keep instrumentation in ccodex-only prompt assembly path"
    recovery: "tighten gating and rerun ccodex-specific coverage"
    owner: "implementation"
