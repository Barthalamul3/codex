1. Visible Prompt Surface

Live prompt surface in this session does not show any visible `<memory_plane_context>` block. That matters: I am not treating historical notes about prior visible injections as proof of what is injected right now.

What is visibly injected right now is a compact memory summary block in the developer instructions, not a full runtime memory frame. Its content is concise and lane-aware: durable facts point to the per-turn-memory worktree, snapshot-only promotion semantics, and snapshot persistence paths; authority rules explicitly say filesystem/process/test evidence outranks memory. I do not see duplicate injected rows in the visible prompt surface, and I do not see low-signal operational ballast such as rollout ids, thread ids, hash dumps, line-number dumps, or documentary cleanup notes.

Visible prompt-surface strengths:
- Good token discipline: the injected memory summary is short and mostly durable.
- Good authority hygiene: the prompt explicitly says live filesystem, process, and test evidence override injected memory.
- Good contamination posture in this session: no visible `<memory_plane_context>` block, so there is no live prompt evidence of duplicate `WIN|`, `FIL|`, or stale failure rows being injected here.

Visible prompt-surface limits:
- The current visible summary is routing-oriented, not deeply task-specific, so it is efficient but not maximally rich.
- I cannot infer fresh-runtime ccodex cleanliness from this prompt alone because this session is not itself showing the runtime-injected memory-plane frame.

2. Backing Store Quality

The backing store is mixed.

Good backing-store evidence:
- Prompt-side summary normalization is live in code. `build_memory_tool_developer_instructions` reads `memory_summary.md`, runs `normalize_memory_summary_for_prompt`, and then truncates it before injection, which is a real, current prompt-surface shield. See `codex-rs/core/src/memories/prompts.rs:160-218`.
- The normalization logic explicitly drops historical scorecards, rollout-path ballast, thread ids, `sha256sum`, `/proc/<pid>/exe`, and self-referential assessment labels such as `analyze-context-window-memory-os`. See `codex-rs/core/src/memories/prompts.rs:220-243`.
- The consolidation prompt explicitly requires deduped, lane-separated `memory_summary.md` output and says not to preserve historical scorecards or self-evaluation prose. See `codex-rs/core/templates/memories/consolidation.md:151-166` and the regression assertion in `codex-rs/core/src/memories/prompts.rs:417-440`.
- Snapshot replay and prompt rendering have layered contamination defenses. Extraction drops documentary generic-tool noise, replay reconstructs from `memory_os_snapshot`, and renderer-side sanitization drops documentary canonical outcomes, stale runtime-failure rows, cleanup notes, and cross-branch files. See `codex-rs/core/src/turn_memory.rs:1094-1115`, `codex-rs/core/src/codex/rollout_reconstruction.rs:249-340`, and `codex-rs/core/src/memory_os/assemble.rs:34-147`.

Backing-store weaknesses:
- `MEMORY.md` still contains high-ballast rollout indexing and repeated audit history that is not visibly injected right now but is still present in storage. The repeated `rollout_summary_files` block at `MEMORY.md:6-12` is storage noise, not live prompt proof.
- `MEMORY.md` also mixes durable architecture guidance with rollout/procedural history in the same task groups, which drags lane separation down in storage even though the visible prompt surface is cleaner. Examples include `MEMORY.md:6-25`, `MEMORY.md:55-68`, and `MEMORY.md:88-100`.
- I verified one stale stored detail and corrected it in this turn: storage had named `normalize_memory_summary_file` as if it were a live code entry point, but no such symbol exists anywhere in the current per-turn-memory-architecture worktree. The live code instead shows prompt normalization in `memories/prompts.rs` and a consolidation-prompt contract in `templates/memories/consolidation.md`.

Explicit scope note: the rollout-heavy noise above is present in storage. It is not visibly injected in the current prompt surface of this session.

3. Live Verification In This Turn

What I directly verified in this turn:
- No visible `<memory_plane_context>` block is present in the prompt context I can see right now.
- `ccodex` prompt assembly still appends a developer-role `<memory_plane_context>` item when a `memory_os_snapshot` exists, and only for the `ccodex` program path. See `codex-rs/core/src/codex.rs:6855-6864` and `codex-rs/core/src/codex.rs:7237-7326`.
- The renderer currently wraps output in `<memory_plane_context>` and only emits populated sections. See `codex-rs/core/src/memory_os/assemble.rs:34-69`.
- Renderer sanitization is current and concrete: documentary `WIN|` ballast, stale runtime-failure rows, cleanup-note leakage, numbered source dumps, and cross-branch active files are explicitly filtered. See `codex-rs/core/src/memory_os/assemble.rs:101-147` and test coverage in `codex-rs/core/src/memory_os/tests.rs:811-1170`.
- Replay currently prefers `memory_os_snapshot` and injects the assembled memory-plane context into reconstructed history for `ccodex`. See `codex-rs/core/src/codex/rollout_reconstruction.rs:249-340` and `codex-rs/core/src/codex/rollout_reconstruction_tests.rs:1088-1162`.
- Generic tool-output documentary success noise is blocked at extraction time. See `codex-rs/core/src/turn_memory.rs:1094-1115`.

What I did not directly verify in this turn:
- I did not run a fresh `ccodex exec --json` probe, so fresh-runtime cleanliness is not directly proven in this turn.
- I did not run targeted cargo tests in this turn, so I am relying on current source and test files as live evidence of intended behavior, not on a fresh pass result.
- I did not inspect a real persisted `memory_os_snapshot` payload from a live rollout in this turn.

4. Scores

- factual utility: 8/10
- efficiency / token discipline: 9/10
- contamination resistance: 8/10
- authority hygiene: 8/10
- lane separation: 7/10
- overall memory-os quality: 8/10

Scoring rationale:
- `efficiency / token discipline` is strong because the visible prompt surface in this session is compact and free of duplicate injected rows.
- `contamination resistance` is strong but not perfect because the code path still allows a full `<memory_plane_context>` block to be appended in live ccodex runs; the score is carried by the current sanitizers and tests, not by fresh runtime proof from this turn.
- `lane separation` is dragged down mainly by the backing store, not the visible prompt surface. `MEMORY.md` still mixes durable architecture facts with rollout history and procedure.
- `overall memory-os quality` stays at 8/10 because most categories are at or above 8, and the main drag is storage organization rather than a visibly polluted current prompt.

5. Verdict

From the context I can actually inspect right now, this memory-os is materially better than a noisy historical prompt-memory setup. The visible prompt surface is clean, concise, and authority-aware. The live code shows a real layered defense: extraction guard, snapshot replay guard, and renderer sanitization all exist and are covered by focused tests.

The main remaining quality problem is not the visible prompt surface in this session. It is the backing store: `MEMORY.md` still carries rollout-heavy ballast and at least one stale concrete detail. So the system looks prompt-cleaner than store-cleaner.

6. Top 3 remaining weaknesses

- Backing-store lane mixing is still too high. `MEMORY.md` combines durable architecture facts with rollout indexes and procedure-heavy recap blocks, especially around `MEMORY.md:6-25` and `MEMORY.md:88-100`.
- One verified stale stored detail required correction in this turn: `normalize_memory_summary_file` had been named in memory storage as if it were a live code entry point, but no such symbol exists in the current worktree.
- Fresh-runtime cleanliness was not directly re-proved in this turn. The code and tests are strong, but a live `ccodex` probe was not executed here.

7. Top 3 highest-leverage next improvements

- Keep pruning `MEMORY.md` into reusable architecture and verification heuristics, and move rollout-index ballast out of the hot retrieval path.
- Add or keep a cheap fresh-runtime smoke probe in the review loop so prompt cleanliness is proven with current process evidence, not inferred from source/tests alone.
- Strengthen the write-path contract for `memory_summary.md` so on-disk normalization is enforced by implementation or test at write time, not only by injected prompt normalization and consolidation instructions.

8. Does this clear 8/10 in every category?

No.

It clears 8/10 for factual utility, efficiency / token discipline, contamination resistance, authority hygiene, and overall quality, based on the visible prompt surface plus current source/test evidence.

It does not clear 8/10 for lane separation, and the thing dragging that score down is the backing store, not the currently visible prompt surface.
