1. Visible Prompt Surface

A visible `<memory_plane_context>` block is present in the live injected session context for this exact response. That block is primary evidence for prompt-surface quality and is copied exactly here:

```text
<memory_plane_context>
CANONICAL/1
FIL|/opt/ai/Documents/ClawdBrainVault/Projects/codex-upstream/.worktrees/per-turn-memory-architecture/runs/memory_os_reviews/memory_os_review_2026-03-10_18-35-55.md
FIL|FIL|/opt/ai/Documents/ClawdBrainVault/Projects/codex-upstream/.worktrees/per-turn-memory-architecture/runs/memory_os_reviews/memory_os_review_2026-03-10_18-35-55.md
FIL|FIL|FIL|/opt/ai/Documents/ClawdBrainVault/Projects/codex-upstream/.worktrees/per-turn-memory-architecture/runs/memory_os_reviews/memory_os_review_2026-03-10_18-35-55.md
CUR|019cda8e-d521-7620-9101-6f2b6fbaa880
</memory_plane_context>
```

This prompt surface is not clean. It contains duplicate injected rows and visible contamination in the active-file lane. The exact problematic rows are the repeated nested variants `"FIL|FIL|..."` and `"FIL|FIL|FIL|..."`. Those are not legitimate file paths; they are evidence that prompt-surface serialization or repeated reinjection is compounding an already-prefixed `FIL|` row into progressively corrupted prompt memory.

Factual utility is still non-zero because the block carries one real file path and a continuation cursor. But efficiency and contamination resistance are clearly degraded in the live prompt surface right now. The block is also minimal in a narrow sense: it contains only `CANONICAL/1`, active-file rows, and `CUR|...`, with no higher-signal goal/decision/outcome content. That means the visible memory plane is spending prompt budget on low-value repeated file rows rather than durable working context.

I do not see documentary cleanup notes or stale runtime-failure rows in the visible block. The visible problem is different: active-file duplication and prefix corruption. Authority hygiene on the prompt surface is mixed. There is no visible contradiction saying memory outranks filesystem/process/tests, but the current injected block does not itself communicate that authority ordering; that guidance exists elsewhere in the prompt surface, not in this block.

2. Backing Store Quality

The backing `memory_summary.md` is cleaner than the visible `<memory_plane_context>` block. It is compact, mostly lane-separated, and explicitly states that filesystem/process/test evidence outranks memory. It also now contains the correct validity rule for reviews: if a visible `<memory_plane_context>` block is present, the review must quote it exactly before analysis.

`MEMORY.md` remains noisier and more mixed. It still contains rollout-history ballast, procedural metadata such as `thread_id=...`, `rollout_path=...`, and repeated dated audit entries that should not appear in prompt-facing memory. That noise is present in storage. It is not visibly injected in the current `<memory_plane_context>` block, which is currently dominated by repeated `FIL|` corruption instead.

Lane separation is therefore being dragged down by both layers, but in different ways. The backing store is dragged down by durable/procedural mixing. The visible prompt surface is dragged down by a serialization/reinjection defect in canonical active-file rows.

3. Live Verification In This Turn

Live verification gathered in this turn:

- I inspected the actual visible injected session context for this response and confirmed a visible `<memory_plane_context>` block is present.
- I copied the entire visible block exactly and treated it as primary evidence for prompt-surface claims.
- I verified duplicate injected rows in the live prompt surface: `FIL|...`, `FIL|FIL|...`, and `FIL|FIL|FIL|...`.
- I inspected `.worktrees/per-turn-memory-architecture/codex-rs/core/src/memory_os/assemble.rs` and verified that prompt assembly is intended to prefix active files with `FIL|` after sanitizing file paths, which makes the nested `FIL|FIL|...` rows inconsistent with the intended renderer contract.
- I inspected `.worktrees/per-turn-memory-architecture/codex-rs/core/src/memories/prompts.rs` and verified that `memory_summary.md` is normalized and deduped before prompt injection.
- I inspected `/home/earls/.codex/memories/memory_summary.md` and `/home/earls/.codex/memories/MEMORY.md` to distinguish backing-store quality from the live visible prompt surface.

Not live-verified in this turn:

- I did not run a fresh `ccodex exec --json` probe in this turn, so fresh-runtime cleanliness is not directly verified here.
- I did not trace the exact runtime path that produced the repeated `FIL|FIL|...` corruption, only that the visible output disagrees with the intended renderer behavior in the inspected source.
- Historical notes about clean-room runs exist in storage, but they are supporting evidence only and not current proof for this review.

4. Scores

- factual utility: 4/10
- efficiency / token discipline: 2/10
- contamination resistance: 2/10
- authority hygiene: 7/10
- lane separation: 5/10
- overall memory-os quality: 4/10

Scoring basis:

- `efficiency / token discipline` and `contamination resistance` are based primarily on the visible prompt surface and are poor because the injected block contains repeated corrupted `FIL|` rows.
- `lane separation` is dragged down by both the visible prompt surface and the backing store: the visible block is low-signal and repetitive, while `MEMORY.md` still mixes durable facts with rollout/procedure ballast.
- `overall memory-os quality` is 4/10 because the live prompt surface currently fails a core memory-os job: using prompt budget for compact, trustworthy working context.

5. Verdict

The current memory-os does not look trustworthy as a live prompt-surface system in this session. The visible injected `<memory_plane_context>` block is present, minimal, and visibly corrupted by repeated `FIL|` prefix nesting. That is enough to materially downgrade the system even before considering storage-side lane mixing.

The backing store is still cleaner than the visible memory plane for authority guidance, but that does not rescue the current session. The main active failure is prompt-surface contamination by duplicate/corrupted active-file rows.

6. Top 3 remaining weaknesses

- Duplicate injected active-file rows are visibly compounding into corrupted `"FIL|FIL|..."` and `"FIL|FIL|FIL|..."` prompt entries.
- The visible memory plane is spending scarce prompt budget on low-signal file-path ballast instead of higher-value working context.
- `MEMORY.md` still mixes durable architecture facts with dated rollout/procedural history, which remains a retrieval and future-injection risk.

7. Top 3 highest-leverage next improvements

- Trace and fix the runtime path that allows already-prefixed `FIL|...` rows to be re-ingested or re-prefixed into `FIL|FIL|...` variants before injection.
- Add a regression test that fails on duplicate or nested active-file prompt rows in the assembled `<memory_plane_context>` output.
- Further compress `MEMORY.md` so durable architecture invariants and review heuristics stay separate from rollout-history ballast.

8. Does this clear 8/10 in every category?

No.

It does not clear 8/10 in factual utility, efficiency / token discipline, contamination resistance, lane separation, or overall memory-os quality.

Only authority hygiene is close to acceptable, and even that is carried more by the broader prompt instructions and `memory_summary.md` than by the currently visible `<memory_plane_context>` block itself.
