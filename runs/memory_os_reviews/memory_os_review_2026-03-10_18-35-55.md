1. Visible Prompt Surface

The live injected session context for this response visibly includes a compact `MEMORY_SUMMARY` block with three sections: `User Profile`, `Durable Facts`, and `Authority Rules`. It does not visibly include any `<memory_plane_context>` block anywhere in the prompt surface I can actually see for this turn, so there is no visible block to copy.

Prompt-surface quality is materially better than the backing store. The visible injected memory is short, mostly factual, and already separated into durable facts versus authority rules. I do not see duplicate injected rows in the visible memory content. I also do not see documentary cleanup notes, runtime hashes, thread IDs, rollout paths, scorecards, or stale failure snippets leaking into the visible injected memory. The highest-signal visible rows are the durable architecture facts around `turn_memory.rs`, `memory_os/assemble.rs`, `normalize_memory_summary_for_prompt`, snapshot-only promotion semantics, and the explicit rule that filesystem/process/test evidence outranks memory.

The main prompt-surface weakness is that the visible block is still a mixed summary rather than a strongly typed lane view. It separates durable facts from authority rules, which is good, but it does not expose explicit fact/evidence/freshness structure, so a future agent still has to infer which lines are architecture invariants versus workflow heuristics. That is a lane-separation limitation in the visible prompt surface, not a contamination event.

2. Backing Store Quality

`memory_summary.md` is substantially cleaner than `MEMORY.md`. It is compact, deduplicated enough to serve as a routing index, and keeps authority-order guidance prominent. That storage layer appears aligned with the intended prompt-surface role.

`MEMORY.md` is still much noisier. It contains substantial rollout-history and procedure ballast that would be expensive if injected directly. Examples of storage-only noise include repeated audit-history entries under `Task 1: Memory-OS context quality scoring workflow...`, operational metadata such as `thread_id=...`, `rollout_path=...`, and runtime-verification details like `/proc/<pid>/exe` and the long runtime hash row beginning `67c2785e...`. Those are not visibly injected right now; they are present in storage only.

The backing store also still mixes durable architecture facts with rollout history and operational procedure more than it should. The `MEMORY.md` block on context quality stores multiple dated review recaps when the reusable artifact should be the rubric and remediation order. That weakens lane separation in storage and would become prompt bloat if retrieval broadens too aggressively.

3. Live Verification In This Turn

Live verification gathered in this turn:

- I inspected the actual visible injected session context available to me for this response.
- I verified that a visible `MEMORY_SUMMARY` block is present in the injected prompt surface.
- I verified that no visible `<memory_plane_context>` block is present in the injected prompt surface I can actually see in this turn.
- I read `/home/earls/.codex/memories/memory_summary.md` and `/home/earls/.codex/memories/MEMORY.md` to compare backing-store quality against the live visible injection.
- I read `.worktrees/per-turn-memory-architecture/codex-rs/core/src/memory_os/assemble.rs` and confirmed live code-level evidence of prompt-surface sanitization, including dropping documentary canonical noise and filtering active files to the current cwd.
- I read `.worktrees/per-turn-memory-architecture/codex-rs/core/src/memories/prompts.rs` and confirmed live code-level evidence that `memory_summary.md` is normalized for prompt use via dedupe/drop rules before injection.
- I read `.worktrees/per-turn-memory-architecture/codex-rs/core/templates/memories/consolidation.md` and confirmed the consolidation contract explicitly requires `memory_summary.md` to remain compact, deduplicated, and lane-oriented.

Not live-verified in this turn:

- Fresh-runtime cleanliness after restart was not directly verified in this turn.
- I did not run a fresh `ccodex exec --json` probe in this turn, so I am not treating stored notes about fresh-session cleanliness as current proof.
- Historical rollout summaries and stored score trends may support context, but they are not equivalent to live verification here.

4. Scores

- factual utility: 8/10
- efficiency / token discipline: 8/10
- contamination resistance: 9/10
- authority hygiene: 9/10
- lane separation: 7/10
- overall memory-os quality: 8/10

Scoring basis:

- `contamination resistance` and `efficiency / token discipline` are based primarily on the visible prompt surface, which is clean and compact in this turn.
- `lane separation` is dragged down more by backing-store structure than by the visible prompt surface. The visible summary is decent; `MEMORY.md` still blends durable facts with dated audit history and procedure.
- `overall memory-os quality` is held at 8/10 because most categories cluster at 8-9 and the live injected surface is materially cleaner than storage.

5. Verdict

The live memory-os visible in this session is in good shape and clearly better than the underlying handbook store. On the thing that actually matters most for this response, the injected prompt surface is compact, factual, non-duplicative, and authority-correct. The system is no longer obviously leaking documentary cleanup notes or stale operational ballast into prompt memory.

The remaining problem is not prompt-surface contamination; it is storage discipline. `MEMORY.md` still carries too much dated review history and operational evidence in the same lane as durable architecture facts. That does not appear to be visibly injected right now, but it remains a retrieval-risk and a future prompt-efficiency risk if selection broadens.

6. Top 3 remaining weaknesses

- `MEMORY.md` still stores repeated dated audit history where a single reusable review heuristic would do.
- Storage lanes are not strict enough: durable architecture facts, review workflow guidance, and operational verification details still coexist in the same handbook blocks.
- Fresh-runtime cleanliness was not re-proved in this turn, so prompt-surface cleanliness is verified only for this current session context, not for all newly spawned sessions.

7. Top 3 highest-leverage next improvements

- Compress the context-quality sections in `MEMORY.md` into one durable rubric/remediation block and remove repeated dated scorecard recaps.
- Tighten handbook lane separation so durable invariants, verification heuristics, and incident-level operational evidence are stored in distinct blocks or schemas.
- Add a cheap, repeatable fresh-runtime verification step to the review workflow so prompt-surface cleanliness can be confirmed in a new session, not inferred from stored notes.

8. Does this clear 8/10 in every category?

No.

It clears 8/10 or better for factual utility, efficiency / token discipline, contamination resistance, authority hygiene, and overall memory-os quality in this turn.

It does not clear 8/10 for lane separation, primarily because the backing store is still too mixed even though the visible injected prompt surface is relatively clean.
