Read this file first and use it as the active handoff:

/opt/ai/Documents/ClawdBrainVault/Projects/codex-upstream/.worktrees/per-turn-memory-architecture/docs/plans/2026-03-08-ccodex-snapshot-reset-handoff.md

Continue from that exact state.

Primary goal:
- preserve the evidence that the current shared-home `memory_plane_context` contamination is live, not historical
- clear the current shared-home session snapshots that may be feeding stale resume state
- restart `ccodex` on the worktree debug binary
- run a fresh minimal two-turn probe on shared-home state
- verify whether the fresh live injected `memory_plane_context` still contains contamination such as:
  - `TRY|...|exec_command`
  - `OBS|...|attempt recorded: exec_command`
  - path-only `attempt failed:` records
  - `FIL|...`
  - `EPIS|...|attempt|exec_command`
  - `KEEP|observational` on raw tool refs
  - test-pass text promoted as `failure`
  - warnings or source-code fragments promoted as episodic failures

Rules:
- do not assume the contamination is old; treat the current injected block as live evidence unless disproven
- capture the latest shared-home rollout path before deleting snapshots
- report exact paths and command evidence for the reset and re-verification
- do not claim the live path is clean unless the fresh post-reset shared-home probe proves it
