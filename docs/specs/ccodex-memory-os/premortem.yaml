run_id: "2026-03-06-ccodex-memory-os"
task_id: "ccodex-memory-os-phase-1"
failures:
  - id: "PM-001"
    severity: "high"
    scenario: "canonical memory diverges from actual current task state"
    detection: "resume/regression tests show stale next step or repeated task-1 planning"
    prevention: "canonical state precedence tests and contradiction records"
    recovery: "rollback to MEM/1-only authority and inspect saved session artifacts"
    owner: "implementation"
  - id: "PM-002"
    severity: "high"
    scenario: "pragmatic inference is treated as fact"
    detection: "tests show inferred items injected without inferred labeling"
    prevention: "separate structs, separate lanes, no shared authority tier"
    recovery: "disable pragmatic injection path and preserve records as non-authoritative"
    owner: "implementation"
  - id: "PM-003"
    severity: "high"
    scenario: "semantic retrieval pollutes continuity"
    detection: "shadow evaluation selects irrelevant records or overwrites next step"
    prevention: "keep retrieval advisory and off by default in phase 1"
    recovery: "feature-gate retrieval and fall back to deterministic planes"
    owner: "implementation"
  - id: "PM-004"
    severity: "medium"
    scenario: "saved session memory becomes uninspectable or too verbose"
    detection: "session artifact review lacks structured memory frame or exceeds compactness targets"
    prevention: "compact structured schema and session artifact assertions"
    recovery: "tighten serializer and strip debug-only fields from durable frames"
    owner: "implementation"
