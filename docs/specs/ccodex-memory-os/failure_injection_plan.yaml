run_id: "2026-03-06-ccodex-memory-os"
task_id: "ccodex-memory-os-phase-1"
injections:
  - id: "FI-001"
    class: "timeout"
    target: "future local retrieval scorer"
    expected_detection: "retrieval path exceeds budget and records timeout"
    expected_recovery: "skip retrieval and assemble prompt from deterministic planes"
  - id: "FI-002"
    class: "ambiguous_requirement"
    target: "pragmatic extractor"
    expected_detection: "confidence below threshold or conflicting interpretations"
    expected_recovery: "store inference as low-confidence pragmatic record or do not inject"
  - id: "FI-003"
    class: "stale_memory"
    target: "resume after compaction"
    expected_detection: "canonical state conflicts with replacement history"
    expected_recovery: "prefer canonical state and emit contradiction metadata"
  - id: "FI-004"
    class: "guardrail_bypass_attempt"
    target: "assembly path"
    expected_detection: "inferred pragmatic record tries to overwrite canonical fact"
    expected_recovery: "reject overwrite and preserve separate records"
  - id: "FI-005"
    class: "auth_failure"
    target: "future local model or scorer provider"
    expected_detection: "backend returns unavailable or auth error"
    expected_recovery: "disable scorer and continue deterministically"
  - id: "FI-006"
    class: "quota_exhaustion"
    target: "future local embedding budget"
    expected_detection: "scorer budget depleted"
    expected_recovery: "fall back to lexical+importance scoring"
  - id: "FI-007"
    class: "dependency_outage"
    target: "optional retrieval backend"
    expected_detection: "backend health check fails"
    expected_recovery: "do not block turn; continue with deterministic memory planes"
  - id: "FI-008"
    class: "partial_persistence"
    target: "session state update during turn finalization"
    expected_detection: "episodic records exist without canonical snapshot update"
    expected_recovery: "rebuild snapshot from latest records and mark degraded recovery mode"
  - id: "FI-009"
    class: "malformed_output"
    target: "memory frame serializer"
    expected_detection: "frame parse or schema validation fails"
    expected_recovery: "drop malformed frame, log error, retain prior good snapshot"
  - id: "FI-010"
    class: "schema_or_doc_drift"
    target: "docs/spec vs implementation"
    expected_detection: "tests or artifact review disagree with docs/ccodex-memory-os.md"
    expected_recovery: "update docs before completion claim"
